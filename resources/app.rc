#include <windows.h>
#include "resource.h"

// Application icon — multi-size ICO embedded by the linker
IDI_APPICON  ICON  "icon.ico"

// Application manifest (UTF-8, Common Controls 6, DPI-aware, per-monitor v2)
1  RT_MANIFEST  "app.manifest"
